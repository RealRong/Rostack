export const whiteboardEdgePresetLabelKey = (
  preset: string
) => `whiteboard.edge.preset.${preset}.label` as const

export const whiteboardInsertPresetLabelKey = (
  preset: string
) => `whiteboard.insert.preset.${preset}.label` as const

export const whiteboardInsertPresetDescriptionKey = (
  preset: string
) => `whiteboard.insert.preset.${preset}.description` as const

export const whiteboardMindmapSeedLabelKey = (
  seed: string
) => `whiteboard.mindmap.seed.${seed}.label` as const

export const whiteboardMindmapSeedDescriptionKey = (
  seed: string
) => `whiteboard.mindmap.seed.${seed}.description` as const

export const whiteboardMindmapPresetLabelKey = (
  preset: string
) => `whiteboard.mindmap.preset.${preset}.label` as const

export const whiteboardMindmapPresetDescriptionKey = (
  preset: string
) => `whiteboard.mindmap.preset.${preset}.description` as const
