以中轴思维为准，看看哪些地方主要有以下几个问题：
1. 从core或者各种helpers文件自己去取然后构造复杂逻辑的
2. 多处重复相似逻辑的
3. 明确可以下沉为core或者engine或者editor逻辑的
4. 可以通过内聚减少对外函数或常量的
5. 触碰了editor不该触碰的领域逻辑或者内部结构的


做一轮完整的代码审计，找出代码中存在的重复可复用逻辑，并明确各自可以分类到shared还是core还是engine，在根目录生成一份完整的迁移清单

做完整的代码审计，目前各种类型散乱在各处，然后各种上下文传
  递也比较混乱，api命名也比较冗长与重复，在根目录生成一份完整的优化迁移清单，目标一步到位
  把engine做到长期最优，代码更加优雅，架构更加健壮


做一轮完整的代码审计，找出core，engine，editor代码中存在的重复可复用逻辑，或重复的类型定义，哪些类型可以只用core提供的，哪些类型是不必要的中间翻译层，在根目录生成一份完整的迁移清单