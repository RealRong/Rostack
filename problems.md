edit field样式不对

现在存在大量input.query.delta.recordSetChanged(input.delta)这种繁琐的字段，然后你觉得`view.order`、`field.options`、`view.display.fields`这些都要成为structure吗，把长期最优方案保存为一份根目录文档，如果你找到其他别扭的地方也可以写到文档里，比如字段设计


edgegudie。cuurent

为什么menu的submenu的submenu，鼠标悬浮上去之后整个就关掉了