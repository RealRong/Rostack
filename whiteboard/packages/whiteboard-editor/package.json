{
  "name": "@whiteboard/editor",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "@shared/core": "workspace:*",
    "@shared/dom": "workspace:*",
    "@shared/projector": "workspace:*",
    "@whiteboard/core": "workspace:*",
    "@whiteboard/editor-graph": "workspace:*",
    "@whiteboard/engine": "workspace:*",
    "@whiteboard/history": "workspace:*"
  },
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts"
  }
}
